#include <stdio.h>

int main()
{
    char str[100];
    char a,b;
    int i;

    fegts(str,100,stdin);
    scanf("%c",&a);
    scanf("%c",&b);

    for (int i = 0; i < 100; i++)
    {
        for (char j = 0; j == str[i]; j++)
        {
            /* code */
        }
            
    
}